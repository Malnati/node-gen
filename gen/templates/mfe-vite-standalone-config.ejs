// mfe-vite-standalone-config.ejs
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [
    react(),
  ],
  build: {
    outDir: 'dist-standalone',
    sourcemap: false,
  },
  server: {
    port: <%= port %>,
  },
  define: {
    'process.env.NODE_ENV': JSON.stringify('production'),
  },
});
