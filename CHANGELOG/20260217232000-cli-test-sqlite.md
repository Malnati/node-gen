<!-- CHANGELOG/20260217232000-cli-test-sqlite.md -->

# 2026-02-17 23:20:00 UTC — Teste do CLI com SQLite em disco

## Arquivos modificados
- `scripts/create-sqlite-fixture.js` — criado (script Node para gerar SQLite em disco com tabela `tb_user`).
- `docs/issues/plan-cli-test-execution.md` — adicionada seção "Teste com SQLite (recomendado)" e exemplos de comando com SQLite.
- `src/utils/ConfigUtil.ts` — removida opção duplicada `-t, --dbType`; incluído `diagram` na descrição de `-f`; cast de `components` usando `DbReaderConfig['components']`.
- `docs/issues/plan-issues-execution.md` — registrada evidência do teste CLI com SQLite.
- `CHANGELOG/20260217232000-cli-test-sqlite.md` — este arquivo.

## Regras e requisitos atendidos
- Plano de execução de testes do CLI ([plan-cli-test-execution.md](docs/issues/plan-cli-test-execution.md)): uso de SQLite em disco para teste sem servidor de banco.
- Correção de defeito: opção duplicada em ConfigUtil impedia execução do CLI; corrigido.
- Evidência de execução registrada em plan-issues-execution.md.

## Comandos executados e resultado
- `node scripts/create-sqlite-fixture.js ./build-cli-test` — sucesso (fixture criada).
- `npm run build` — sucesso.
- `node dist/main.js ... -t sqlite -d ./build-cli-test/fixture.sqlite -o ./build-cli-test -f "entities,services,...,diagram"` — sucesso (todos os componentes gerados).

## Resultado resumido
- CLI testado com SQLite em disco; geração concluída sem exceção; artefatos verificados (entities, services, interfaces, controllers, dtos, modules, app-module, main, env, package.json, readme, datasource, diagram). In-memory não utilizado (banco vazio quando o CLI abre `:memory:`); documentado no plano como limitação.
